## building

Commands are defined in the makefile use `make help` to see them 

## UI and Styling

- use [daisyUI components](https://daisyui.com/llms.txt) for all UI elements (buttons, modals, cards, inputs).
- use `ui/static/styles/base.css` for theme references